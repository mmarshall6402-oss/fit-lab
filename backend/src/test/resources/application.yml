spring:
  datasource:
    url: jdbc:h2:mem:fitlab-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

fitlab:
  upload-dir: ${java.io.tmpdir}/fitlab-uploads-test
  admin:
    token: test-admin-token
