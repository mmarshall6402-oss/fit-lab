# fitcheck

FIT//LAB - a streetwear fit-builder app.

- `backend/` - Spring Boot REST API (catalog, matching, outfit scoring). See `backend/README.md`.
- `frontend/` - React + TypeScript UI. See `frontend/README.md`.